name = "gym-tracker-worker"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put` or GitHub Actions):
#   GITHUB_TOKEN
#   GITHUB_OWNER
#   GITHUB_REPO
#   ALLOWED_ORIGIN
